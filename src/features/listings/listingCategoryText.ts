/**
 * Copy for the shared listing category strip.
 */
export const listingCategoryText = {
  title: 'Categories',
  empty: 'No categories found',
  loading: 'Loading categories',
  retry: 'Try again',
} as const;
