/**
 * Application navigation.
 *
 * The gallery is the only surface. No routing package yet — add one when
 * deep links or auth gates appear.
 */
export const appRouter = {
  gallery: '/',
} as const;
