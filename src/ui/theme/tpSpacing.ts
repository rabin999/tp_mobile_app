/**
 * 4-based spacing scale.
 *
 * Observed mobile UI uses 8 as the MUI default unit plus 12/16/20 gutters.
 */
export const tpSpacing = {
  xxs: 4,
  xs: 8,
  sm: 12,
  md: 16,
  lg: 20,
  xl: 24,
  xxl: 32,
} as const;
